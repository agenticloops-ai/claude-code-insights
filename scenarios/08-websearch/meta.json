{
  "name": "08-websearch",
  "description": "Single-prompt knowledge question whose only practical answer requires fresh web data \u2014 the agent has to decide on WebSearch itself. WebSearch is pre-approved so the call isn't blocked in non-interactive -p mode. Captures the full round-trip a version uses to discover and call its search tool.",
  "claude_args": [
    "--allowedTools",
    "WebSearch",
    "-p"
  ],
  "mcp": null,
  "env": {}
}
